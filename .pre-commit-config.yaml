repos:
  - repo: local
    hooks:
      - id: commitlint
        name: commitlint
        entry: npx --yes @commitlint/cli --config commitlint.config.js --edit
        language: system
        stages: [commit-msg]
        pass_filenames: true
